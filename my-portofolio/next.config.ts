import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: false, // disable the dev tools
  output: "export",
  basePath: "/my-portofolio",
};

export default nextConfig;
